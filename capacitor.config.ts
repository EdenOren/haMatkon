import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hamatkon.app',
  appName: 'haMatkon',
  webDir: 'dist/ha-matkon/browser',
};

export default config;
